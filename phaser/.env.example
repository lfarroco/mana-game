# RETIRED — the Supabase backend was replaced by the Node game server (server/).
# See the repo-root .env.example for the current environment variables
# (MANA_STEAM_WEB_API_KEY, MANA_SERVER_URL, MANA_ITCH_CLIENT_ID, ...).
