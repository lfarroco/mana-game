version: '3.8'

services:
  db:
    build: .
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: mana_battle
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
